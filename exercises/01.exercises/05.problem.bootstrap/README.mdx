# Bootstrap Modules

Problem: Get client-side JavaScript on the page

You're going to create a file at <InlineFile file="src/index.js" /> (click that
to create the file), and add a `console.log` in it to be sure it's being loaded
properly when you're finished.
